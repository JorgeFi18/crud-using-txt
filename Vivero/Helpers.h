#include <string>
using namespace std;

string serializeString(string str, bool reverse = false);
string removeBreak(string str);
bool validateAnswer(string answer);
string getNombrePlanta(int tipoPlanta);
void gotoxy(int x, int y);